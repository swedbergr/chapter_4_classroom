# Initiate named constants
ROWS = 4
COLUMNS = 5

# Iterate for every row
for r in range(ROWS):
  # Iterate for every column
  for c in range(COLUMNS):
    print("*", end = "")

  # End row and make new line
  print("")